json.extract! section, :id, :classNumber, :meetings, :campus, :waitlistTotal, :term, :endDate, :startDate, :enrollmentStatus, :instructionMode, :component, :section, :created_at, :updated_at
json.url section_url(section, format: :json)
